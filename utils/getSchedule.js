const data = require()
async function getSchedule(interaction) {
    let logs = "```css\n";
    /* if (!interaction.user.bot) {
        await interaction.delete();
    }; */
    roles = interaction.member.roles.cache;
    if (roles.has("942445906832281652")) {
        console.log(1);
    }
}

module.exports = {
    getSchedule,
}